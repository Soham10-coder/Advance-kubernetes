apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: d3
  name: d3
spec:
  replicas: 3
  selector:
      app: d3
  template:
    metadata:
      labels:
        app: d3
    spec:
      containers:
      - image: nginx
        name: nginx
      
